# 再開メモ（2026-07-14 更新）

## 状態（すべて未コミット）
- **FV刷新（jojodesign.jp踏襲）完了**: 参照サイトの配置・考え方を、自前コピー/パステル配色/HIROロゴで再現（jojoの文字造形・写真・文言そのものは非複製）。①ヘッダーを中央寄せ浮遊ピル化（ロゴ上/●区切りナビ下、`.site-header`をflex-column・left:50%+translateX、900pxのsite-header上書き削除、768pxで行レイアウトに戻す）②中央に回転キーワードマーク（アウトライン初期文字＋語＋パルスドット、DESIGN/CODE/MOTION/STORYを2.9s巡回）＝`initHeroKeyword()`③見出しを左下配置（`.hero .container`をgrid rows:1fr auto、`.hero-copy`をalign-self:end/justify-self:start）④既存particle-canvasのconstellationを背景活用。h1は`clamp(38px,5.4vw,64px)`に縮小、kicker「AI×DESIGN×CODE」はHTMLから削除。デスクトップ検証OK
- **reveal発火バグ修正**: reveal-rotateの3D回転(rotateY-150deg)投影ボックスが画面外→IO閾値0.14未達でFVコピーが不可視だった。`initScrollReveal`でロード時にビューポート内(rect.top<innerHeight*0.9)要素は即is-visible付与、他はobserve
- **未検証**: モバイル実機（この環境でウィンドウ縮小が効かず未目視。モバイルCSSは記述済み）。ローダーはjojo完全複製せず既存splash(HIRO CODE描画)を流用
- **既存挙動メモ**: ヘッダーは`.site-shell`のsiteEnter(forwards→transform残留)が包含ブロックになりスクロールで退避（回帰ではない・jojo同様の消える挙動）
- **パステルトンマナ差し替え完了**: 額縁=パステルグラデ、アクセント=くすみブルー#4A6FA5、背景=クリーム#FAFAFA。検収・検品済み、指摘3件修正済み（スクリム濃色化 rgba(43,63,94,…)・PRIMARY_RGB統一・未使用変数削除）
- **MV人物写真→ABOUT移設完了**: ヒーローはタイポ主体（h1上限96px・lead 60ch・1カラム）、ABOUTは見出し→写真左（チルトカード・キャプション付き）→本文右583px。768px以下は見出し→写真→本文の縦積み。差し戻し1回（本文列173px問題）で解消。検収・検品合格
- **ABOUT見出し1行化完了**: `.about .section-title` に grid-column: 1 / -1 を追加。「はじめまして、Hiroです。」がPCで3行→1行（64px・幅1000px）に。ローカル確認済み
- Formspree実送信テスト合格（無料プランは_nextリダイレクト無視でthanksページ遷移が仕様）
- ホスティング: **Xserver**（hiro-portfolio.com）。※deploy zipはユーザー判断で不要のため削除済み。デプロイ方法は未確定

## 検品の残指摘（低優先・未対応）
- frame-copyのAA境界コントラスト／額縁コーナー継ぎ目（Winスクロールバー・モバイルURLバー、視認困難レベル）／theme-color#4A6FA5と額縁ピンクの不一致／OGP画像・faviconが旧配色前提の可能性

## 触ったファイル
index.html / css/style.css / js/main.js（全て未コミット）

## 次の一手
1. ユーザー: 実ブラウザで最終目視（特にモバイル実機のABOUT縦積み）→ OKならコミット依頼
2. ユーザー: テストメール受信確認（件名「ポートフォリオサイトからのお問い合わせ」）
3. デプロイ方法の確定（Xserverへの反映手段。zip展開／FTP／git等）
4. 公開後にClaude Codeで本番URL検収
