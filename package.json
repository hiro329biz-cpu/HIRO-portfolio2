{
  "name": "hiro-portfolio",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/check-site.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
